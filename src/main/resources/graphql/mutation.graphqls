# All mutations available in graphql
type Mutation {
    #exam
    addExam(authorId: ID!, input: ExamInput!): Exam!
    updateExam(id: ID!, authorId: ID!, input: ExamInput!): Exam!
    publishExam(id: ID!, authorId: ID!): Exam!
    cancelExam(id: ID!, authorId: ID!): Exam!
}